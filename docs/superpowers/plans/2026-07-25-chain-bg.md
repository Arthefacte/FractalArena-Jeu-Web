# Fond blockchain vivante — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fond canvas permanent : chaîne de blocs octogonaux qui « se mine » en boucle (cycle 8 s) derrière l'UI, opacité 0,1, flash teinté par l'accent d'écran.

**Architecture:** Module pur `chain-bg-ui.js` (`FA_CHAIN_BG_UI` : `cycleVals`/`blockGeom`/`hexPair`, code du proto Claude Design validé) ; canvas singleton `chain-bg.js` (`FA_CHAIN_BG.mount()` idempotent, 24 fps, pause si onglet caché, reduced-motion → frame statique) ; mount depuis `Ambient` (app.jsx).

**Tech Stack:** Vanilla JS (IIFE, pas de build), canvas 2D, tests `node --test`.

## Global Constraints

- Spec : `docs/superpowers/specs/2026-07-25-chain-bg-design.md`. Branche `chain-bg` (worktree `wt-chain-bg`), base origin/main v87.
- Client seulement — AUCUN changement serveur. Aucune entrée CSP. Aucun son.
- Cache-busting : bump global `?v=87` → `?v=89` dans `index.html` (tâche 3 uniquement — on saute v88, réservé à la PR #58 moment-forge, pour éviter toute collision de version).
- Tests : `node --test --test-force-exit test/*.test.js` (⚠️ toujours `--test-force-exit`). Le worktree exige `npm install` au préalable (sinon `market-parse.test.js` échoue sur `@babel/standalone` — pré-existant, pas lié à nous).
- Scripts classiques IIFE (modèle `forge-ui.js`). Pas de `Math.random()` dans le rendu — angle d'or (`i * 2.399963`).
- L'existant (`.app-bg`, braises, filigrane) ne change PAS ; en cas d'erreur du module → teardown silencieux, fond actuel intact.
- Messages de commit : suffixe `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: `chain-bg-ui.js` — module pur (cycle + géométrie)

**Files:**
- Create: `chain-bg-ui.js`
- Test: `test/chain-bg-ui.test.js`

**Interfaces:**
- Produces: `window.FA_CHAIN_BG_UI = { GOLD, cycleVals, blockGeom, hexPair }`
  - `cycleVals(t: 0..1) → { pulse, hexTick, flash, spark, slide, mined }`
  - `blockGeom(i: int) → { dy, scale, rot }` (déterministe par index)
  - `hexPair(i, tick) → string` (2 chars hex)

- [ ] **Step 1: Écrire le test qui échoue**

Créer `test/chain-bg-ui.test.js` :

```js
const test = require("node:test");
const assert = require("node:assert");
globalThis.window = {};
require("../chain-bg-ui.js");
const U = globalThis.window.FA_CHAIN_BG_UI;

test("cycleVals : flash nul avant la découverte (t=0,74), présent juste après", () => {
  assert.strictEqual(U.cycleVals(0.5).flash, 0);
  assert.strictEqual(U.cycleVals(0.5).mined, false);
  assert.ok(U.cycleVals(0.75).flash > 0.5, "flash fort juste après 0,74");
  assert.strictEqual(U.cycleVals(0.75).mined, true);
  assert.ok(U.cycleVals(0.99).flash < 0.01, "flash éteint en fin de cycle");
});

test("cycleVals : slide nul avant 0,8, monte vers 1 en fin de cycle", () => {
  assert.strictEqual(U.cycleVals(0.79).slide, 0);
  assert.ok(U.cycleVals(0.9).slide > 0.3);
  assert.ok(U.cycleVals(0.999).slide > 0.95);
});

test("cycleVals : spark -1 avant la découverte, 0..1 après", () => {
  assert.strictEqual(U.cycleVals(0.5).spark, -1);
  const s = U.cycleVals(0.78).spark;
  assert.ok(s >= 0 && s <= 1);
});

test("cycleVals : pulse et hexTick varient dans le cycle", () => {
  assert.notStrictEqual(U.cycleVals(0.1).pulse, U.cycleVals(0.15).pulse);
  assert.strictEqual(U.cycleVals(0.5).hexTick, 15);
});

test("blockGeom : déterministe et borné", () => {
  assert.deepStrictEqual(U.blockGeom(42), U.blockGeom(42));
  for (const i of [0, 1, 7, 100, 12345]) {
    const g = U.blockGeom(i);
    assert.ok(Math.abs(g.dy) <= 6.001, "dy borné");
    assert.ok(g.scale >= 0.9 && g.scale <= 1.04001, "scale borné");
    assert.ok(Math.abs(g.rot) <= 0.08001, "rot borné");
  }
  assert.notDeepStrictEqual(U.blockGeom(1), U.blockGeom(2), "varie par index");
});

test("hexPair : 2 caractères hex, déterministe", () => {
  for (const [i, t] of [[0, 0], [3, 7], [99, 12]]) {
    const h = U.hexPair(i, t);
    assert.match(h, /^[0-9a-f]{2}$/);
    assert.strictEqual(h, U.hexPair(i, t));
  }
});

test("pureté : mêmes entrées → mêmes sorties", () => {
  assert.deepStrictEqual(U.cycleVals(0.42), U.cycleVals(0.42));
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `node --test --test-force-exit test/chain-bg-ui.test.js`
Expected: FAIL (`Cannot find module '../chain-bg-ui.js'`)

- [ ] **Step 3: Implémenter `chain-bg-ui.js`** (état pur du proto validé, tel quel)

```js
/* FRACTAL ARENA — Fond blockchain vivante : état pur du cycle de minage (testable Node).
   Aucun DOM, aucun aléa, aucune horloge. Cycle t ∈ [0,1) : minage → découverte (t=0,74,
   flash+étincelles) → glissement d'un cran (t=0,8→1) pendant que le bloc suivant entre. */
(function () {
  "use strict";

  const GOLD = 2.399963; // angle d'or — variations déterministes

  function cycleVals(t) {
    const FOUND = 0.74, SLIDE = 0.8;
    const pulse = 0.5 + 0.5 * Math.sin(t * Math.PI * 10);
    const hexTick = Math.floor(t * 30);
    const d = t - FOUND;
    const flash = d < 0 ? 0 : Math.exp(-(d * d) / 0.0018);
    const spark = d < 0 ? -1 : Math.min(d / 0.14, 1);
    let slide = 0;
    if (t >= SLIDE) { const u = (t - SLIDE) / (1 - SLIDE); slide = u * u * (3 - 2 * u); }
    return { pulse, hexTick, flash, spark, slide, mined: t >= FOUND };
  }

  function blockGeom(i) {
    const g = (((i * GOLD) % (Math.PI * 2)) + Math.PI * 2) % (Math.PI * 2);
    return { dy: Math.sin(g) * 6, scale: 0.9 + 0.14 * (0.5 + 0.5 * Math.cos(g * 1.7)), rot: Math.sin(g * 2.3) * 0.08 };
  }

  function hexPair(i, tick) {
    const f = ((((i * 137 + tick * 29) * GOLD) % 1) + 1) % 1;
    return Math.floor(f * 256).toString(16).padStart(2, "0");
  }

  const api = { GOLD, cycleVals, blockGeom, hexPair };
  if (typeof window !== "undefined") window.FA_CHAIN_BG_UI = api;
})();
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `node --test --test-force-exit test/chain-bg-ui.test.js` → PASS (7 tests)

- [ ] **Step 5: Suite complète + commit**

Run: `node --test --test-force-exit test/*.test.js` → tout PASS, puis :

```bash
git add chain-bg-ui.js test/chain-bg-ui.test.js
git commit -m "feat(chain-bg): module pur FA_CHAIN_BG_UI — cycle de minage + geometrie des blocs

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: `chain-bg.js` — canvas de fond singleton

**Files:**
- Create: `chain-bg.js`
- Test: `test/chain-bg.test.js`

**Interfaces:**
- Consumes: `window.FA_CHAIN_BG_UI` (Task 1).
- Produces: `window.FA_CHAIN_BG = { mount }` — `mount()` idempotent, crée le canvas de fond et démarre la boucle. Repli : si `FA_CHAIN_BG_UI` absent → no-op.

- [ ] **Step 1: Écrire le test qui échoue** (niveau source)

Créer `test/chain-bg.test.js` :

```js
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");

const src = fs.readFileSync(path.join(__dirname, "..", "chain-bg.js"), "utf8");

test("expose FA_CHAIN_BG.mount", () => {
  assert.match(src, /window\.FA_CHAIN_BG\s*=\s*\{\s*mount\s*\}/);
});

test("mount idempotent (garde sur l'instance)", () => {
  assert.match(src, /if \(mounted\) return/);
});

test("boucle plafonnée à ~24 fps", () => {
  assert.match(src, /1000 \/ 24/);
});

test("pause quand l'onglet est caché", () => {
  assert.match(src, /visibilitychange/);
  assert.match(src, /document\.hidden/);
});

test("reduced-motion → une frame statique, pas de boucle", () => {
  assert.match(src, /prefers-reduced-motion/);
});

test("accent du flash lu une fois par cycle via --accent, repli cyan", () => {
  assert.match(src, /--accent/);
  assert.match(src, /getComputedStyle/);
  assert.match(src, /#00F0FF|0,240,255/);
});

test("robustesse : try/catch + teardown (canvas retiré)", () => {
  assert.match(src, /catch/);
  assert.match(src, /remove\(\)/);
});

test("déterminisme : pas de Math.random, angle d'or", () => {
  assert.ok(!/Math\.random/.test(src));
  assert.match(src, /2\.399963|GOLD/);
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `node --test --test-force-exit test/chain-bg.test.js`
Expected: FAIL (ENOENT)

- [ ] **Step 3: Implémenter `chain-bg.js`** (dessin du proto + garde-fous jeu)

```js
/* FRACTAL ARENA — Fond blockchain vivante : canvas fixe derrière l'UI (au-dessus de .app-bg,
   sous les braises z-1). L'état de chaque frame vient de FA_CHAIN_BG_UI (pur) ; ici on ne fait
   que dessiner. Fond PERMANENT → sobriété stricte : ~24 fps, pause onglet caché, DPR ≤ 1,5,
   reduced-motion = une frame statique. Erreur de rendu → teardown, le fond statique reste. */
(function () {
  "use strict";
  if (typeof window === "undefined") return;

  const OPACITY = 0.1, CYCLE_MS = 8000, BLOCK_START = 841200;
  let mounted = false, cv = null, g2 = null, raf = 0;
  let phase = 0, cycles = 0, last = 0, lastDraw = -1e9;
  let accent = "0,240,255"; // rgb du flash — relu à chaque nouveau cycle
  let accentCycle = -1;

  function rgbOf(hex) {
    const h = String(hex || "#00F0FF").trim().replace("#", "");
    if (!/^[0-9a-fA-F]{6}$/.test(h)) return "0,240,255";
    return parseInt(h.slice(0, 2), 16) + "," + parseInt(h.slice(2, 4), 16) + "," + parseInt(h.slice(4, 6), 16);
  }
  function readAccent() {
    try {
      const v = getComputedStyle(document.body).getPropertyValue("--accent");
      accent = rgbOf(v || "#00F0FF");
    } catch (e) { accent = "0,240,255"; }
  }

  function oct(ctx, x, y, r, rot) {
    ctx.beginPath();
    for (let k = 0; k < 8; k++) {
      const a = rot + Math.PI / 8 + k * Math.PI / 4;
      const vx = x + Math.cos(a) * r, vy = y + Math.sin(a) * r;
      if (k) ctx.lineTo(vx, vy); else ctx.moveTo(vx, vy);
    }
    ctx.closePath();
  }
  function drawMined(ctx, x, y, r, rot, i, a) {
    ctx.fillStyle = "rgba(0,150,190," + (0.09 * a) + ")";
    ctx.strokeStyle = "rgba(0,220,245," + (0.42 * a) + ")";
    ctx.lineWidth = 1.1;
    oct(ctx, x, y, r, rot); ctx.fill(); ctx.stroke();
    ctx.font = Math.max(7, Math.round(r * 0.26)) + 'px "JetBrains Mono", monospace';
    ctx.textAlign = "center"; ctx.textBaseline = "middle";
    ctx.fillStyle = "rgba(0,220,245," + (0.3 * a) + ")";
    ctx.fillText("#" + (BLOCK_START + i), x, y);
  }
  function drawMining(ctx, x, y, r, rot, i, pulse, hexTick, a) {
    const U = window.FA_CHAIN_BG_UI;
    ctx.strokeStyle = "rgba(247,147,26," + ((0.45 + 0.4 * pulse) * a) + ")";
    ctx.lineWidth = 1.4 + 1.1 * pulse;
    oct(ctx, x, y, r, rot); ctx.stroke();
    ctx.strokeStyle = "rgba(247,147,26," + (0.16 * pulse * a) + ")";
    ctx.lineWidth = 5;
    oct(ctx, x, y, r + 4, rot); ctx.stroke();
    ctx.font = Math.max(8, Math.round(r * 0.3)) + 'px "JetBrains Mono", monospace';
    ctx.textAlign = "center"; ctx.textBaseline = "middle";
    ctx.fillStyle = "rgba(247,147,26," + ((0.55 + 0.3 * pulse) * a) + ")";
    for (let row = 0; row < 2; row++) {
      let s = "";
      for (let col = 0; col < 3; col++) s += U.hexPair(i * 7 + row * 3 + col, hexTick) + (col < 2 ? " " : "");
      ctx.fillText(s, x, y + (row - 0.5) * r * 0.52);
    }
  }

  function draw() {
    const U = window.FA_CHAIN_BG_UI;
    const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
    const W = cv.clientWidth, H = cv.clientHeight;
    if (!W || !H) return;
    if (cv.width !== Math.round(W * dpr) || cv.height !== Math.round(H * dpr)) {
      cv.width = Math.round(W * dpr); cv.height = Math.round(H * dpr);
    }
    g2.setTransform(dpr, 0, 0, dpr, 0, 0);
    g2.clearRect(0, 0, W, H);

    const v = U.cycleVals(phase);
    if (cycles !== accentCycle) { accentCycle = cycles; readAccent(); }
    const size = Math.max(22, Math.min(H * 0.052, 46));
    const spacing = size * 2.7;
    const headX = W * 0.86, baseY = H * 0.8;
    const count = Math.ceil((headX + spacing) / spacing) + 1;
    const head = cycles;
    const px = (k) => headX - (k + v.slide) * spacing;
    const fade = (x) => Math.max(0, Math.min(1, (x + size) / (spacing * 1.3)));

    g2.lineWidth = 1; // liens sous les blocs
    for (let k = count; k >= 0; k--) {
      const iA = head - k; if (iA < 0) continue;
      const gA = U.blockGeom(iA), gB = U.blockGeom(iA + 1);
      const xA = px(k), xB = px(k - 1);
      let la = fade(xA) * 0.28;
      if (k === 0) la *= v.slide;
      if (la <= 0.004) continue;
      g2.strokeStyle = "rgba(0,220,245," + la + ")";
      g2.beginPath();
      g2.moveTo(xA + size * gA.scale * 0.95, baseY + gA.dy);
      g2.lineTo(xB - size * gB.scale * 0.95, baseY + gB.dy);
      g2.stroke();
    }
    for (let k = count; k >= 0; k--) { // blocs
      const i = head - k; if (i < 0) continue;
      const g = U.blockGeom(i);
      const x = px(k), y = baseY + g.dy, r = size * g.scale;
      const a = fade(x);
      if (a <= 0) continue;
      if (k === 0 && !v.mined) drawMining(g2, x, y, r, g.rot, i, v.pulse, v.hexTick, 1);
      else drawMined(g2, x, y, r, g.rot, i, a);
    }
    if (v.slide > 0.001) { // bloc entrant par la droite
      const i = head + 1, g = U.blockGeom(i);
      drawMining(g2, px(-1), baseY + g.dy, size * g.scale, g.rot, i, 0.3 * v.slide, 0, v.slide);
    }
    if (v.flash > 0.01 || (v.spark >= 0 && v.spark < 1)) { // flash + étincelles (accent)
      const g = U.blockGeom(head);
      const x = px(0), y = baseY + g.dy, r = size * g.scale;
      if (v.flash > 0.01) {
        const R = r * (2.2 + 1.8 * (1 - v.flash));
        const grad = g2.createRadialGradient(x, y, 0, x, y, R);
        grad.addColorStop(0, "rgba(" + accent + "," + (0.55 * v.flash) + ")");
        grad.addColorStop(1, "rgba(" + accent + ",0)");
        g2.fillStyle = grad;
        g2.fillRect(x - R, y - R, R * 2, R * 2);
      }
      if (v.spark >= 0 && v.spark < 1) {
        const GOLD = window.FA_CHAIN_BG_UI.GOLD;
        for (let j = 0; j < 8; j++) {
          const ang = j * GOLD + head * 0.7;
          const fr = (((j * GOLD) % 1) + 1) % 1;
          const dist = r * (1 + v.spark * (1.6 + 1.4 * fr));
          const sa = (1 - v.spark) * (1 - v.spark);
          g2.fillStyle = "rgba(" + accent + "," + (0.8 * sa) + ")";
          g2.fillRect(x + Math.cos(ang) * dist - 1, y + Math.sin(ang) * dist * 0.65 - 1, 2, 2);
        }
      }
    }
  }

  function teardown() {
    if (raf) { cancelAnimationFrame(raf); raf = 0; }
    if (cv) { try { cv.remove(); } catch (e) {} }
    cv = null; g2 = null;
  }

  function step(ts) {
    try {
      raf = requestAnimationFrame(step);
      if (ts - lastDraw < 1000 / 24 - 2) return;
      const dt = Math.min(ts - last, 100);
      last = ts; lastDraw = ts;
      const cyc = CYCLE_MS;
      phase += dt / cyc;
      while (phase >= 1) { phase -= 1; cycles++; }
      draw();
    } catch (e) { teardown(); }
  }

  function mount() {
    if (mounted) return;
    mounted = true;
    try {
      if (!window.FA_CHAIN_BG_UI || !document.body) return;
      cv = document.createElement("canvas");
      cv.style.cssText = "position:fixed;inset:0;width:100%;height:100%;z-index:0;pointer-events:none;opacity:" + OPACITY + ";";
      document.body.appendChild(cv);
      g2 = cv.getContext("2d");
      const reduced = !!(window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
      if (reduced) { phase = 0.3; try { draw(); } catch (e) { teardown(); } return; } // une frame statique
      last = performance.now();
      document.addEventListener("visibilitychange", () => {
        if (document.hidden) { if (raf) { cancelAnimationFrame(raf); raf = 0; } }
        else if (cv && !raf) { last = performance.now(); lastDraw = -1e9; raf = requestAnimationFrame(step); }
      });
      raf = requestAnimationFrame(step);
    } catch (e) { teardown(); }
  }

  window.FA_CHAIN_BG = { mount };
})();
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `node --test --test-force-exit test/chain-bg.test.js` → PASS (8 tests), puis suite complète.

- [ ] **Step 5: Commit**

```bash
git add chain-bg.js test/chain-bg.test.js
git commit -m "feat(chain-bg): canvas de fond FA_CHAIN_BG — 24 fps, pause onglet cache, reduced-motion statique

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Intégration — `app.jsx` (Ambient) + `index.html` (v89)

**Files:**
- Modify: `app.jsx` (`Ambient`, ligne ~1357 — ajout d'un `useEffect` de mount)
- Modify: `index.html` (bloc scripts, + bump global `?v=87` → `?v=89`)
- Test: `test/chain-bg-wiring.test.js`

**Interfaces:**
- Consumes: `window.FA_CHAIN_BG.mount()` (Task 2).

- [ ] **Step 1: Écrire le test qui échoue**

Créer `test/chain-bg-wiring.test.js` :

```js
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");

const html = fs.readFileSync(path.join(__dirname, "..", "index.html"), "utf8");
const app = fs.readFileSync(path.join(__dirname, "..", "app.jsx"), "utf8");

test("index.html charge chain-bg-ui.js puis chain-bg.js (scripts classiques)", () => {
  const a = html.indexOf('<script src="chain-bg-ui.js');
  const b = html.indexOf('<script src="chain-bg.js');
  assert.ok(a > -1, "chain-bg-ui.js chargé");
  assert.ok(b > a, "chain-bg.js après chain-bg-ui.js");
  assert.ok(!/type="module" src="chain-bg/.test(html), "scripts classiques");
});

test("cache-busting v89, plus aucun v87/v88", () => {
  assert.ok(html.includes("?v=89"));
  assert.ok(!html.includes("?v=87") && !html.includes("?v=88"));
});

test("Ambient monte le fond avec repli silencieux", () => {
  const m = app.match(/function Ambient\(\)[\s\S]{0,600}FA_CHAIN_BG\?\.mount\(\)/);
  assert.ok(m, "FA_CHAIN_BG?.mount() dans Ambient");
});

test("l'existant reste : .app-bg et braises inchangés", () => {
  assert.match(app, /className="app-bg"/);
  assert.match(app, /className="embers"/);
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `node --test --test-force-exit test/chain-bg-wiring.test.js` → FAIL (4 tests)

- [ ] **Step 3: Modifier `app.jsx`** — dans `Ambient()` (ligne ~1357), ajouter le mount AVANT le `return` :

```jsx
  // Fond blockchain vivante (esthétique #5 volet 2) — repli silencieux si module absent
  useEffect(() => { window.FA_CHAIN_BG?.mount(); }, []);
```

(La fonction utilise déjà `useMemo` importé en tête de fichier ; `useEffect` y est aussi.)

- [ ] **Step 4: Modifier `index.html`** — après la ligne `<script src="cosmetic.js?v=87"></script>`, insérer :

```html
  <script src="chain-bg-ui.js?v=87"></script>
  <script src="chain-bg.js?v=87"></script>
```

Puis bump global (PowerShell, racine du worktree) :

```powershell
(Get-Content index.html -Raw) -replace '\?v=87', '?v=89' | Set-Content index.html -NoNewline
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `node --test --test-force-exit test/chain-bg-wiring.test.js` → PASS, puis suite complète (`npm install` d'abord si pas fait) → tout PASS.

- [ ] **Step 6: Commit**

```bash
git add app.jsx index.html test/chain-bg-wiring.test.js
git commit -m "feat(ambient): fond blockchain vivante monte depuis Ambient, cache-bust v89

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Page proto + vérification navigateur

**Files:**
- Create: `_chain-bg-proto.html` (démo locale, préfixe `_` = hors prod)

- [ ] **Step 1: Créer `_chain-bg-proto.html`**

```html
<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="utf-8">
<title>Proto — Fond blockchain vivante</title>
<style>
  body { margin:0; background:#05070f; color:#eaf4ff; font-family:monospace; min-height:100vh; }
  .fake { position:relative; z-index:2; margin:40px auto; width:520px; padding:24px;
          background:#0d1420cc; border:1px solid #2a3446; }
  #hud { position:fixed; top:8px; left:8px; z-index:3; font-size:12px; color:#8a8f98; }
</style>
</head>
<body style="--accent:#B026FF">
<div id="hud">accent violet — le flash du prochain bloc doit être violet</div>
<div class="fake"><h3>Panneau factice</h3><p>Vérifier que la chaîne reste discrète sous l'UI,
et que le flash « bloc trouvé » (toutes les 8 s) prend la couleur --accent du body.</p>
<button onclick="document.body.style.setProperty('--accent','#27E08A'); document.getElementById('hud').textContent='accent vert — prochain flash vert';">Accent → vert</button></div>
<script src="chain-bg-ui.js"></script>
<script src="chain-bg.js"></script>
<script>window.FA_CHAIN_BG.mount(); window.FA_CHAIN_BG.mount(); /* idempotence */</script>
</body>
</html>
```

- [ ] **Step 2: Vérification navigateur**

Servir le worktree (`python -m http.server 8124`) puis :
1. Ouvrir `http://localhost:8124/_chain-bg-proto.html` — la chaîne apparaît en bande basse, discrète, UN seul canvas dans le DOM (idempotence du double mount).
2. Attendre un cycle (~8 s) : flash violet au bloc trouvé, glissement, nouveau bloc.
3. Cliquer « Accent → vert » : le flash suivant est vert.
4. Console : zéro erreur.
5. Ouvrir `http://localhost:8124/` (app v89) : boot OK, zéro erreur console, chaîne visible derrière l'UI.

- [ ] **Step 3: Commit**

```bash
git add _chain-bg-proto.html
git commit -m "chore(chain-bg): page proto _chain-bg-proto.html

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

- [ ] **Step 4: Montrer au user, puis PR** — après son verdict visuel : push `chain-bg`, PR vers main.

---

## Self-Review (fait à l'écriture du plan)

- Couverture spec : module pur (T1), canvas + perf/robustesse (T2), intégration + v89 (T3), vérif navigateur + idempotence + accent (T4). ✓
- Types cohérents : `cycleVals/blockGeom/hexPair` (T1) consommés à l'identique dans T2 ; `mount()` (T2) consommé dans T3/T4. ✓
- Pas de placeholder : tout le code est écrit, y compris les tests. ✓
- v89 (pas v88) partout — collision avec la PR #58 évitée ; si #58 est mergée avant, `?v=88` sera présent sur main : le rebase remplacera alors `\?v=88` → `\?v=89` (le test wiring l'attrape). ✓
