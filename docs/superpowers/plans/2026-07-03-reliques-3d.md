# Reliques 3D (art réel) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer les icônes de reliques procédurales (primitives Three.js) par de vrais modèles 3D `.glb`, avec vignettes statiques partout et un viewer interactif à l'invocation et en détail d'inventaire.

**Architecture:** Un module pur `relic-assets.js` (chemins + table de glow par rareté, node-testable) ; un module Three.js `relic-models.js` qui charge/cache les `.glb` (GLTFLoader), normalise l'échelle, et produit des instances teintées par rareté ; `relic-icons.js` refactoré pour rendre des vignettes à partir des modèles (repli primitive tant que le `.glb` n'est pas chargé) ; un composant React `relic-viewer.jsx` pour le canvas 3D live. La couleur du modèle = identité du type ; la rareté = halo émissif (couleur + intensité), pas un recolorage.

**Tech Stack:** Vanilla JS + Three.js 0.160 (importmap `three` / `three/addons/`), React 18 via Babel-in-browser (`.jsx`), tests `node:test`.

## Global Constraints

- Tests : `node --test test/*.test.js` — JAMAIS `node --test test/` (dossier casse sur Node v24).
- Aucune nouvelle dépendance npm côté client (pas de build step ; Three via importmap, React via Babel navigateur).
- `window.FA_RELIC_ICON.get(type, rarity, size)` conserve sa signature synchrone → `dataURL | null`.
- Discipline GPU stricte : `dispose()` de géométrie/matériaux/textures/renderer au démontage (réf. régression `fix/web-webgl-leak`).
- 8 types : `ruby_shard, sapphire_plate, quartz_lens, amber_cell, cobalt_spring, onyx_membrane, jade_circuit, prism_matrix`.
- Rareté = glow émissif préservant la couleur du modèle. Table (couleur/intensité) : Common `0x9CA3AF`/0.15, Rare `0x3B82F6`/0.35, Epic `0xB026FF`/0.6, Legendary `0xF7931A`/0.95.
- Chemin d'asset : `assets/relics/<type>.glb`.
- Repli en cascade, jamais d'écran vide : `.glb` → primitive Three.js actuelle → pastille losange.
- Cache-bust global → **v60** dans `index.html` au déploiement.
- Fichiers `.glb` fournis par l'utilisateur dans `assets/relics/` (Option B — export Meshy). Prépa d'optimisation faite hors plan quand ils arrivent.

---

### Task 1: `relic-assets.js` — données pures (chemins + rareté)

**Files:**
- Create: `relic-assets.js`
- Test: `test/relic-assets.test.js`

**Interfaces:**
- Produces: `window.FA_RELIC_ASSETS` / `module.exports` = `{ TYPES: string[], path(type): string, RARITY_GLOW: {[rarity]: {color: number, intensity: number}} }`

- [ ] **Step 1: Write the failing test**

```js
// test/relic-assets.test.js
const test = require("node:test");
const assert = require("node:assert");
const A = require("../relic-assets.js");

test("TYPES : les 8 types de reliques", () => {
  assert.deepStrictEqual([...A.TYPES].sort(), [
    "amber_cell", "cobalt_spring", "jade_circuit", "onyx_membrane",
    "prism_matrix", "quartz_lens", "ruby_shard", "sapphire_plate",
  ]);
});

test("path : chemin .glb par type", () => {
  assert.strictEqual(A.path("ruby_shard"), "assets/relics/ruby_shard.glb");
  assert.strictEqual(A.path("prism_matrix"), "assets/relics/prism_matrix.glb");
});

test("RARITY_GLOW : 4 raretés, intensité croissante", () => {
  const order = ["Common", "Rare", "Epic", "Legendary"];
  order.forEach((r) => {
    assert.ok(typeof A.RARITY_GLOW[r].color === "number", `${r} color`);
    assert.ok(A.RARITY_GLOW[r].intensity >= 0 && A.RARITY_GLOW[r].intensity <= 1, `${r} intensity`);
  });
  for (let i = 1; i < order.length; i++) {
    assert.ok(A.RARITY_GLOW[order[i]].intensity > A.RARITY_GLOW[order[i - 1]].intensity,
      `${order[i]} > ${order[i - 1]}`);
  }
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/relic-assets.test.js`
Expected: FAIL — `Cannot find module '../relic-assets.js'`

- [ ] **Step 3: Write minimal implementation**

```js
// relic-assets.js
/* ============================================================
   FRACTAL ARENA — Reliques 3D : données pures (chemins + rareté).
   Global navigateur (window.FA_RELIC_ASSETS) + export Node.
   ============================================================ */
(function () {
  "use strict";

  const TYPES = [
    "ruby_shard", "sapphire_plate", "quartz_lens", "amber_cell",
    "cobalt_spring", "onyx_membrane", "jade_circuit", "prism_matrix",
  ];

  function path(type) { return "assets/relics/" + type + ".glb"; }

  // Rareté = halo émissif (couleur + intensité). Préserve la couleur du modèle.
  const RARITY_GLOW = {
    Common:    { color: 0x9CA3AF, intensity: 0.15 },
    Rare:      { color: 0x3B82F6, intensity: 0.35 },
    Epic:      { color: 0xB026FF, intensity: 0.60 },
    Legendary: { color: 0xF7931A, intensity: 0.95 },
  };

  const api = { TYPES, path, RARITY_GLOW };
  if (typeof window !== "undefined") window.FA_RELIC_ASSETS = api;
  if (typeof module !== "undefined" && module.exports) module.exports = api;
})();
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/relic-assets.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add relic-assets.js test/relic-assets.test.js
git commit -m "feat(2.2): relic-assets — chemins .glb + table de glow par rareté"
```

---

### Task 2: Test de garde des assets `.glb`

**Files:**
- Test: `test/relic-glb-assets.test.js`
- Depends on: les 8 `.glb` déposés dans `assets/relics/` par l'utilisateur (Option B).

**Interfaces:**
- Consumes: `FA_RELIC_ASSETS.TYPES` (Task 1)

- [ ] **Step 1: Write the failing test**

```js
// test/relic-glb-assets.test.js
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const A = require("../relic-assets.js");

const MAX_BYTES = 1_500_000; // budget par modèle (GitHub Pages) — 1.5 MB

for (const type of A.TYPES) {
  test(`asset présent et sous budget : ${type}.glb`, () => {
    const p = path.join(__dirname, "..", "assets", "relics", `${type}.glb`);
    assert.ok(fs.existsSync(p), `manquant : ${p}`);
    const size = fs.statSync(p).size;
    assert.ok(size > 0, `vide : ${type}.glb`);
    assert.ok(size <= MAX_BYTES, `${type}.glb = ${size} o > ${MAX_BYTES} o (à optimiser)`);
  });
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/relic-glb-assets.test.js`
Expected: FAIL — assets manquants (tant que les `.glb` ne sont pas déposés/optimisés)

- [ ] **Step 3: Déposer + optimiser les assets**

Les 8 `.glb` sont fournis par l'utilisateur dans `assets/relics/`. Si un fichier dépasse 1,5 MB, l'optimiser hors client (décimation + weld/dedup, SANS compression Draco pour éviter un décodeur côté client) :

```bash
npx --yes @gltf-transform/cli optimize assets/relics/<type>.glb assets/relics/<type>.glb \
  --compress false --simplify true --texture-compress webp
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/relic-glb-assets.test.js`
Expected: PASS (8 tests)

- [ ] **Step 5: Commit**

```bash
git add test/relic-glb-assets.test.js assets/relics/*.glb
git commit -m "feat(2.2): 8 modèles .glb des reliques + test de garde (présence + budget)"
```

---

### Task 3: `relic-models.js` — chargement/cache/instances Three.js

**Files:**
- Create: `relic-models.js`

**Interfaces:**
- Consumes: `FA_RELIC_ASSETS.path`, `FA_RELIC_ASSETS.RARITY_GLOW` (Task 1) ; `three`, `three/addons/loaders/GLTFLoader.js`
- Produces: `window.FA_RELIC_MODELS` = `{ preload(): void, isReady(type): boolean, makeInstance(type, rarity): THREE.Object3D, loadModel(type): Promise<void> }`
  - `makeInstance` : lève une erreur si `!isReady(type)` (le repli est géré par l'appelant).
  - Émet `window` event `fa:relic-model-ready` avec `{ detail: { type } }` à la fin de chaque chargement.

- [ ] **Step 1: Write the module**

Pas de test unitaire (Three/WebGL non testable sous `node:test`) — vérification navigateur en Task 4/6.

```js
// relic-models.js — chargement + cache + instances teintées des modèles de reliques.
import * as THREE from "three";
import { GLTFLoader } from "three/addons/loaders/GLTFLoader.js";

(function () {
  const A = window.FA_RELIC_ASSETS;
  const loader = new GLTFLoader();
  const _loaded = {};   // type -> THREE.Group (normalisé, matériaux d'origine)
  const _loading = {};  // type -> Promise

  // Centre le modèle sur l'origine et le met à l'échelle pour tenir dans une boîte ~2 unités.
  function _normalize(root) {
    const box = new THREE.Box3().setFromObject(root);
    const size = box.getSize(new THREE.Vector3());
    const center = box.getCenter(new THREE.Vector3());
    const maxDim = Math.max(size.x, size.y, size.z) || 1;
    root.position.sub(center);
    const s = 2 / maxDim;
    const wrap = new THREE.Group();
    wrap.add(root);
    wrap.scale.setScalar(s);
    return wrap;
  }

  function loadModel(type) {
    if (_loaded[type]) return Promise.resolve();
    if (_loading[type]) return _loading[type];
    _loading[type] = new Promise((resolve) => {
      loader.load(
        A.path(type),
        (gltf) => {
          _loaded[type] = _normalize(gltf.scene);
          delete _loading[type];
          window.dispatchEvent(new CustomEvent("fa:relic-model-ready", { detail: { type } }));
          resolve();
        },
        undefined,
        () => { delete _loading[type]; resolve(); } // échec silencieux → l'appelant garde le repli
      );
    });
    return _loading[type];
  }

  function preload() { A.TYPES.forEach(loadModel); }
  function isReady(type) { return !!_loaded[type]; }

  // Clone le modèle + ses matériaux, applique le glow de rareté (préserve la couleur/texture).
  function makeInstance(type, rarity) {
    if (!_loaded[type]) throw new Error("relic model not ready: " + type);
    const inst = _loaded[type].clone(true);
    const glow = A.RARITY_GLOW[rarity] || A.RARITY_GLOW.Common;
    inst.traverse((o) => {
      if (!o.isMesh || !o.material) return;
      const mats = Array.isArray(o.material) ? o.material : [o.material];
      o.material = mats.map((m) => {
        const c = m.clone();
        if (c.emissive) { c.emissive = new THREE.Color(glow.color); c.emissiveIntensity = glow.intensity; }
        return c;
      });
      if (!Array.isArray(o.material)) o.material = o.material[0];
    });
    return inst;
  }

  window.FA_RELIC_MODELS = { preload, isReady, makeInstance, loadModel };
})();
```

- [ ] **Step 2: Commit**

```bash
git add relic-models.js
git commit -m "feat(2.2): relic-models — chargement/cache GLTF + instances teintées par rareté"
```

---

### Task 4: `relic-icons.js` — vignettes depuis les modèles + repli primitive

**Files:**
- Modify: `relic-icons.js`

**Interfaces:**
- Consumes: `FA_RELIC_MODELS.isReady/makeInstance/loadModel` (Task 3), `FA_RELIC_ASSETS.RARITY_GLOW` (Task 1)
- Produces: `window.FA_RELIC_ICON.get(type, rarity, size) → dataURL | null` (signature inchangée)

- [ ] **Step 1: Réécrire le rendu de `get()`**

Conserver le renderer partagé et le cache dataURL existants. Modifier `get()` pour : si le modèle est prêt → rendre l'instance du modèle ; sinon → lancer le chargement et rendre la primitive actuelle (repli), sans cacher le repli sous une clé définitive.

```js
// relic-icons.js — vignettes des reliques : modèle .glb si prêt, sinon primitive (repli).
import * as THREE from "three";
(function () {
  const CACHE = new Map();               // "type|rarity|size" -> dataURL (modèle uniquement)
  const RARITY_HEX = { Common: 0x9CA3AF, Rare: 0x3B82F6, Epic: 0xB026FF, Legendary: 0xF7931A };
  let renderer = null, scene = null, camera = null, key = null, amb = null, ok = true;

  function geoFor(type) {
    switch (type) {
      case "ruby_shard":     return new THREE.OctahedronGeometry(1);
      case "sapphire_plate": return new THREE.BoxGeometry(1.3, 1.3, 0.55);
      case "quartz_lens":    return new THREE.IcosahedronGeometry(1);
      case "amber_cell":     return new THREE.DodecahedronGeometry(1);
      case "cobalt_spring":  return new THREE.TorusGeometry(0.75, 0.3, 14, 28);
      case "onyx_membrane":  return new THREE.TetrahedronGeometry(1.25);
      case "jade_circuit":   return new THREE.TorusKnotGeometry(0.6, 0.22, 80, 10);
      case "prism_matrix":   return new THREE.ConeGeometry(1, 1.7, 6);
      default:               return new THREE.OctahedronGeometry(1);
    }
  }

  function init() {
    if (renderer) return true;
    try {
      renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true, preserveDrawingBuffer: true });
      scene = new THREE.Scene();
      camera = new THREE.PerspectiveCamera(38, 1, 0.1, 100);
      camera.position.set(0, 0, 4);
      key = new THREE.DirectionalLight(0xffffff, 1.4); key.position.set(2, 3, 4); scene.add(key);
      amb = new THREE.AmbientLight(0xffffff, 0.55); scene.add(amb);
      return true;
    } catch (e) { ok = false; return false; }
  }

  function _renderObject(obj, size) {
    renderer.setSize(size, size);
    scene.add(obj);
    renderer.render(scene, camera);
    const url = renderer.domElement.toDataURL("image/png");
    scene.remove(obj);
    return url;
  }

  function _primitive(type, rarity) {
    const hex = RARITY_HEX[rarity] || 0x9CA3AF;
    const geo = geoFor(type);
    const mat = new THREE.MeshStandardMaterial({ color: hex, emissive: hex, emissiveIntensity: 0.4, metalness: 0.6, roughness: 0.25 });
    const mesh = new THREE.Mesh(geo, mat);
    mesh.rotation.set(0.5, 0.7, 0);
    return { obj: mesh, dispose: () => { geo.dispose(); mat.dispose(); } };
  }

  function _model(type, rarity) {
    const inst = window.FA_RELIC_MODELS.makeInstance(type, rarity);
    inst.rotation.set(0.35, 0.7, 0);
    // Géométrie/textures PARTAGÉES avec le modèle en cache (clone() ne les copie pas) → NE PAS disposer.
    // Seuls les matériaux sont clonés dans makeInstance → on ne dispose qu'eux.
    const dispose = () => inst.traverse((o) => {
      if (o.isMesh) {
        const mats = Array.isArray(o.material) ? o.material : [o.material];
        mats.forEach((m) => m && m.dispose());
      }
    });
    return { obj: inst, dispose };
  }

  function get(type, rarity, size) {
    size = size || 56;
    if (!ok) return null;
    const k = type + "|" + rarity + "|" + size;
    if (CACHE.has(k)) return CACHE.get(k);
    if (!init()) return null;

    const M = window.FA_RELIC_MODELS;
    try {
      if (M && M.isReady(type)) {
        const { obj, dispose } = _model(type, rarity);
        const url = _renderObject(obj, size);
        dispose();
        CACHE.set(k, url);          // seul le rendu modèle est mis en cache définitivement
        return url;
      }
      if (M) M.loadModel(type);     // lance le chargement ; re-render à l'event ready
      const { obj, dispose } = _primitive(type, rarity);
      const url = _renderObject(obj, size);
      dispose();
      return url;                    // repli non caché
    } catch (e) { ok = false; return null; }
  }

  if (window.FA_RELIC_MODELS) window.FA_RELIC_MODELS.preload();
  window.FA_RELIC_ICON = { get };
})();
```

- [ ] **Step 2: Vérification navigateur (nécessite les `.glb`)**

Servir le client localement, aller sur Forge → onglet Reliques → inventaire. Attendu : les icônes affichent d'abord la primitive puis basculent sur le vrai modèle une fois chargé. Console sans fuite WebGL (pas de warning « Too many active WebGL contexts »).

- [ ] **Step 3: Commit**

```bash
git add relic-icons.js
git commit -m "feat(2.2): vignettes reliques depuis les modèles .glb (repli primitive si non chargé)"
```

---

### Task 5: `components.jsx` — re-render de `RelicIcon` à l'event ready

**Files:**
- Modify: `components.jsx` (fonction `RelicIcon`, ~L157-164)

**Interfaces:**
- Consumes: event `window` `fa:relic-model-ready` (Task 3)

- [ ] **Step 1: Abonner `RelicIcon` à l'event ready**

```jsx
// Icône 3D des reliques (2.1/2.2) — vignette Three.js (modèle .glb), repli primitive/pastille
function RelicIcon({ type, rarity, size }) {
  const s = size || 28;
  const [, force] = useState(0);
  useEffect(() => {
    const onReady = (e) => { if (!e.detail || e.detail.type === type) force((n) => n + 1); };
    window.addEventListener("fa:relic-model-ready", onReady);
    return () => window.removeEventListener("fa:relic-model-ready", onReady);
  }, [type]);
  const url = (window.FA_RELIC_ICON && window.FA_RELIC_ICON.get(type, rarity, s * 2)) || null;
  if (url) return <img src={url} alt="" width={s} height={s} draggable="false" style={{ display: "inline-block", verticalAlign: "middle" }} />;
  const col = (window.FA_DATA && window.FA_DATA.RARITY_COLORS[rarity]) || "#9CA3AF";
  return <span style={{ width: Math.round(s * 0.45), height: Math.round(s * 0.45), display: "inline-block", background: col, clipPath: "polygon(50% 0,100% 50%,50% 100%,0 50%)" }} />;
}
```

Vérifier que `useState`/`useEffect` sont disponibles dans le scope de `components.jsx` (destructurés depuis `React` en tête de fichier — les ajouter au besoin).

- [ ] **Step 2: Vérification navigateur (nécessite les `.glb`)**

Recharger l'inventaire des reliques : les vignettes passent de la primitive au modèle sans interaction (le re-render suit l'event).

- [ ] **Step 3: Commit**

```bash
git add components.jsx
git commit -m "feat(2.2): RelicIcon se re-rend quand le modèle .glb est prêt"
```

---

### Task 6: `relic-viewer.jsx` — viewer 3D interactif

**Files:**
- Create: `relic-viewer.jsx`

**Interfaces:**
- Consumes: `FA_RELIC_MODELS.loadModel/isReady/makeInstance` (Task 3), `three`
- Produces: `window.RelicViewer` = composant React `<RelicViewer type rarity size />`

- [ ] **Step 1: Écrire le composant**

Canvas Three.js live : rotation auto + drag souris. `dispose()` strict au démontage (géométrie, matériaux, renderer, `forceContextLoss`). Un seul viewer monté à la fois en pratique.

```jsx
// relic-viewer.jsx — viewer 3D interactif d'une relique (rotation auto + drag), dispose GPU strict.
function RelicViewer({ type, rarity, size }) {
  const px = size || 220;
  const ref = React.useRef(null);
  React.useEffect(() => {
    const THREE = window.__FA_THREE; // exposé par relic-viewer-boot (import module)
    if (!THREE || !window.FA_RELIC_MODELS) return;
    const mount = ref.current;
    const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
    renderer.setSize(px, px);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2));
    mount.appendChild(renderer.domElement);
    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(38, 1, 0.1, 100);
    camera.position.set(0, 0, 4.2);
    const key = new THREE.DirectionalLight(0xffffff, 1.4); key.position.set(2, 3, 4); scene.add(key);
    scene.add(new THREE.AmbientLight(0xffffff, 0.55));

    let obj = null, raf = 0, dragging = false, lastX = 0, autoRot = true, disposed = false;
    function mountObj() {
      obj = window.FA_RELIC_MODELS.makeInstance(type, rarity);
      obj.rotation.set(0.3, 0.6, 0);
      scene.add(obj);
    }
    function ensure() {
      if (window.FA_RELIC_MODELS.isReady(type)) { mountObj(); }
      else { window.FA_RELIC_MODELS.loadModel(type).then(() => { if (!disposed && window.FA_RELIC_MODELS.isReady(type)) mountObj(); }); }
    }
    ensure();
    function loop() {
      raf = requestAnimationFrame(loop);
      if (obj && autoRot && !dragging) obj.rotation.y += 0.012;
      renderer.render(scene, camera);
    }
    loop();

    const el = renderer.domElement;
    const onDown = (e) => { dragging = true; autoRot = false; lastX = e.clientX; };
    const onMove = (e) => { if (dragging && obj) obj.rotation.y += (e.clientX - lastX) * 0.01, lastX = e.clientX; };
    const onUp = () => { dragging = false; };
    el.addEventListener("pointerdown", onDown);
    window.addEventListener("pointermove", onMove);
    window.addEventListener("pointerup", onUp);

    return () => {
      disposed = true;
      cancelAnimationFrame(raf);
      el.removeEventListener("pointerdown", onDown);
      window.removeEventListener("pointermove", onMove);
      window.removeEventListener("pointerup", onUp);
      // Géométrie/textures partagées avec le cache → NE PAS disposer. Seuls les matériaux clonés.
      if (obj) obj.traverse((o) => {
        if (o.isMesh) {
          const mats = Array.isArray(o.material) ? o.material : [o.material];
          mats.forEach((m) => m && m.dispose());
        }
      });
      renderer.dispose();
      renderer.forceContextLoss();
      if (el.parentNode) el.parentNode.removeChild(el);
    };
  }, [type, rarity, px]);

  return <div ref={ref} style={{ width: px, height: px, margin: "0 auto" }} />;
}
window.RelicViewer = RelicViewer;
```

- [ ] **Step 2: Exposer THREE au scope non-module**

`relic-viewer.jsx` tourne sous Babel (non-module) et ne peut pas `import`. Créer un petit bootstrap module qui expose THREE :

Create `relic-viewer-boot.js` :
```js
import * as THREE from "three";
window.__FA_THREE = THREE;
```

- [ ] **Step 3: Vérification navigateur (nécessite les `.glb`)**

Voir Task 7 pour le point de montage. Attendu : modèle qui tourne, drag fonctionnel, aucune fuite WebGL au démontage (ouvrir/fermer la modale 10×, vérifier l'absence de warning « Too many active WebGL contexts »).

- [ ] **Step 4: Commit**

```bash
git add relic-viewer.jsx relic-viewer-boot.js
git commit -m "feat(2.2): RelicViewer — canvas 3D interactif + dispose GPU strict"
```

---

### Task 7: `screens.jsx` — viewer à l'invocation + modale de détail inventaire

**Files:**
- Modify: `screens.jsx` (`ForgeReliques` ~L346-394 : panneau de résultat + cartes d'inventaire)

**Interfaces:**
- Consumes: `window.RelicViewer` (Task 6), `Modal` (existant)

- [ ] **Step 1: Viewer dans le panneau de résultat de l'invocation**

Remplacer l'icône statique 48px du bloc `last` par le viewer :

```jsx
// dans ForgeReliques, bloc `last` (remplace <RelicIcon ... size={48} />)
<div style={{ margin: "0 auto 12px", display: "flex", justifyContent: "center" }}>
  {window.RelicViewer
    ? <RelicViewer type={last.type} rarity={last.rarity} size={200} />
    : <RelicIcon type={last.type} rarity={last.rarity} size={48} />}
</div>
```

- [ ] **Step 2: Modale de détail depuis l'inventaire**

Ajouter un state d'ouverture + rendre une carte cliquable qui ouvre une `Modal` avec le viewer. Dans `ForgeReliques`, ajouter en tête `const [detail, setDetail] = useState(null);`, rendre chaque carte d'inventaire cliquable `onClick={() => setDetail(inst)}`, et après la grille :

```jsx
{detail && (
  <Modal onClose={() => setDetail(null)}>
    <div style={{ textAlign: "center", padding: 8 }}>
      {window.RelicViewer && <RelicViewer type={detail.type} rarity={detail.rarity} size={240} />}
      <div style={{ fontWeight: 700, fontSize: 16, marginTop: 10 }}>{I18N.t("RELIC_" + detail.type.toUpperCase())}</div>
      <div style={{ color: D.RARITY_COLORS[detail.rarity], fontWeight: 600, marginTop: 4 }}>{rarityLabel(detail.rarity)}</div>
      <div className="mono muted" style={{ fontSize: 13, marginTop: 8 }}>{D.relicStatDelta(D.relicEffect(detail.type, detail.rarity))}</div>
    </div>
  </Modal>
)}
```

Vérifier la signature réelle de `Modal` dans `components.jsx` (props `onClose`/`children`) et adapter si besoin.

- [ ] **Step 3: Vérification navigateur (nécessite les `.glb`)**

Forge → Reliques → invoquer : le modèle tourne dans le panneau de résultat. Cliquer une carte d'inventaire : la modale ouvre le viewer ; fermer : pas de fuite WebGL.

- [ ] **Step 4: Commit**

```bash
git add screens.jsx
git commit -m "feat(2.2): viewer 3D à l'invocation + modale de détail d'inventaire"
```

---

### Task 8: `index.html` — scripts + cache-bust v60

**Files:**
- Modify: `index.html`

- [ ] **Step 1: Ajouter les nouveaux scripts + bumper la version**

Dans la section « plain JS », après `relic-icons.js` (module), ajouter `relic-assets.js` (avant `relic-icons.js` car il en dépend) et les modules :

```html
<script src="relic-assets.js?v=60"></script>
<script type="module" src="relic-viewer-boot.js?v=60"></script>
<script type="module" src="relic-models.js?v=60"></script>
<script type="module" src="relic-icons.js?v=60"></script>
```

Dans la section React (Babel), après `campaign.jsx` :
```html
<script type="text/babel" src="relic-viewer.jsx?v=60"></script>
```

Ordre de dépendance à respecter : `relic-assets.js` → `relic-models.js` → `relic-icons.js`. Puis passer **toutes** les occurrences `?v=59` (et les nouveaux) à `?v=60`.

- [ ] **Step 2: Bump global de la version**

```bash
sed -i -E 's/\?v=59"/?v=60"/g' index.html
```

- [ ] **Step 3: Vérifier tous les assets + tous les tests**

```bash
node --test test/*.test.js
for f in $(grep -oE 'src="[^"]+\?v=' index.html | sed -E 's/src="//;s/\?v=//'); do [ -f "$f" ] && echo "OK  $f" || echo "MANQUANT $f"; done
```
Expected : tests PASS (incl. relic-assets + relic-glb-assets) ; tous les fichiers OK.

- [ ] **Step 4: Vérification navigateur finale (les 3 replis)**

1. `.glb` présents → modèles partout + viewer live.
2. Simuler `.glb` absent (renommer un fichier) → primitive s'affiche, pas d'écran vide.
3. WebGL désactivé → pastille losange.

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "chore(2.2): scripts reliques 3D + cache-bust v59→v60"
```

---

## Notes de déploiement

- Tout part sur la branche `feat/equipement-reliques-client` (PR #31) : reliques + 3D en un seul merge.
- Les `.png` de référence (`art/relics/refs/`) sont de la source, non servie au jeu → à ajouter au `.gitignore` (garder en local). Les `.glb` (`assets/relics/`) SONT servis → committés.
- Le merge de #31 = mise en prod fractalarena.com. Décision du merge = utilisateur.
- Chantier B (Marché entre joueurs) = spec/plan séparés, après A.
